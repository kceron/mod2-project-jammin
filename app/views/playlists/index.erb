<h1>Playlists!</h1>

<%= form_tag playlists_path, method: :get do %>
  <%= text_field_tag :search, params[:search], list: "name" %>
  <datalist id="name">
    <% Playlist.all.each do |playlist| %>
      <option value="<%= playlist.name %>"></option>
    <% end %>
  </datalist>
  <%= submit_tag "Search", name: nil %>
<% end %>

